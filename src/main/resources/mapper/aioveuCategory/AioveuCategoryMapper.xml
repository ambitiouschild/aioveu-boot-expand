<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aioveu.boot.aioveuCategory.mapper.AioveuCategoryMapper">

    <!-- 获取物资分类分页列表 -->
    <select id="getAioveuCategoryPage" resultType="com.aioveu.boot.aioveuCategory.model.vo.AioveuCategoryVO">
        SELECT
                id,
                name,
                parent_id,
                level,
                description,
                create_time,
                update_time
        FROM
            aioveu_category
        <where>
            <if test="queryParams.id != null">
                AND id LIKE CONCAT('%', #{queryParams.id}, '%')
            </if>
            <if test="queryParams.name != null and queryParams.name != ''">
                AND name LIKE CONCAT('%', #{queryParams.name}, '%')
            </if>
            <if test="queryParams.description != null and queryParams.description != ''">
                AND description LIKE CONCAT('%', #{queryParams.description}, '%')
            </if>
        </where>
    </select>

</mapper>
