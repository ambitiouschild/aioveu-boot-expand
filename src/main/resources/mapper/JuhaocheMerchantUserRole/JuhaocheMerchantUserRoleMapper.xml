<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aioveu.boot.JuhaocheMerchantUserRole.mapper.JuhaocheMerchantUserRoleMapper">

    <!-- 获取商户用户角色分页列表 -->
    <select id="getJuhaocheMerchantUserRolePage" resultType="com.aioveu.boot.JuhaocheMerchantUserRole.model.vo.JuhaocheMerchantUserRoleVO">
        SELECT
                id,
                user_id,
                merchant_id,
                role_id,
                is_active,
                grant_time,
                grant_user_id,
                expire_time,
                is_enable,
                create_time,
                create_user,
                update_time,
                update_user,
                is_del
        FROM
            juhaoche_merchant_user_role
        <where>
        </where>
    </select>

</mapper>
